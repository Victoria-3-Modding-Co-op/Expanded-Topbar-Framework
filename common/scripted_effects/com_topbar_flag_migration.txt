migrated_com_topbar_elements_to_flag = {
	every_country = {
		every_in_list = {
			variable = com_topbar_first_line
			limit = {
				# is it a struct
				OR = {
					exists = home_country
					has_variable = is_struct
				}
			}
			prev = {
				add_to_variable_list = {
					name = com_topbar_first_line
					target = prev.var:com_name
				}
				remove_list_variable = {
					name = com_topbar_first_line
					target = prev
				}
			}
		}
		every_in_list = {
			variable = com_topbar_second_line
			limit = {
				# is it a struct
				OR = {
					exists = home_country
					has_variable = is_struct
				}
			}
			prev = {
				add_to_variable_list = {
					name = com_topbar_second_line
					target = prev.var:com_name
				}
				remove_list_variable = {
					name = com_topbar_second_line
					target = prev
				}
			}
		}
	}
	every_in_global_list = {
		variable = com_topbar_items
		limit = {
			# is it a struct
			OR = {
				exists = home_country
				has_variable = is_struct
			}
		}
		add_to_global_variable_list = {
			name = com_topbar_items
			target = var:com_name
		}
		remove_list_global_variable = {
			name = com_topbar_items
			target = this
		}
		set_character_immortal = no
		free_character_from_void = yes
		kill_character = {
			hidden = yes
			value = yes
		}
	}
	set_global_variable = com_topbar_migration_complete
}